# Production Notes

This repository copy is prepared for public upload.
Store local configuration in environment variables or a local `.env` file.
Keep runtime data and cache files out of version control.
